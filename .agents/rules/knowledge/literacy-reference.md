# リテラシー適応リファレンス

> literacy-adaptation.md の詳細リファレンス。エージェントが必要に応じて参照する。

---

## 用語の説明ガイド

### 開発・プログラミング用語

| 専門用語 | beginner | intermediate |
|----------|----------|--------------|
| リポジトリ | **リポジトリ**（プロジェクトの保管場所） | **リポジトリ**（保管場所） |
| コミット | **コミット**（変更を記録すること） | **コミット**（変更記録） |
| プッシュ | **プッシュ**（変更をサーバーに送ること） | **プッシュ**（サーバーへ送信） |
| プルリクエスト | **プルリクエスト**（変更の確認依頼、PRとも呼びます） | **PR**（変更確認依頼） |
| ブランチ | **ブランチ**（作業用の分岐、本流を汚さずに作業できます） | **ブランチ**（作業用分岐） |
| マージ | **マージ**（変更を統合すること） | **マージ**（統合） |
| デプロイ | **デプロイ**（作ったものを公開して使えるようにすること） | **デプロイ**（公開） |
| API | **API**（システム同士が会話する仕組み） | **API**（外部連携の窓口） |
| エンドポイント | **エンドポイント**（データを取りに行く場所のURL） | **エンドポイント**（接続先URL） |
| データベース | **データベース**（情報を保存しておく場所、DBとも呼びます） | **DB**（データ保存場所） |
| クエリ | **クエリ**（データを探す命令） | **クエリ**（検索命令） |
| ログ | **ログ**（システムの記録・履歴） | **ログ**（記録） |
| バグ | **バグ**（プログラムの不具合） | **バグ**（不具合） |
| デバッグ | **デバッグ**（問題を見つけて直す作業） | **デバッグ**（問題修正） |
| リファクタリング | **リファクタリング**（動きを変えずにコードを整理整頓すること） | **リファクタリング**（コード整理） |
| テスト | **テスト**（ちゃんと動くか自動で確認する仕組み） | **テスト**（動作確認） |
| 環境変数 | **環境変数**（パスワードなど、コードに直接書かない設定値） | **環境変数**（設定値） |
| 認証 | **認証**（「あなたは誰？」を確認すること） | **認証**（本人確認） |
| 認可 | **認可**（「何をしていい？」の許可を与えること） | **認可**（権限付与） |

### インフラ・ネットワーク用語

| 専門用語 | beginner | intermediate |
|----------|----------|--------------|
| サーバー | **サーバー**（24時間動いているコンピュータ） | **サーバー**（常時稼働PC） |
| クラウド | **クラウド**（インターネット上のコンピュータを借りるサービス） | **クラウド**（ネット上のサーバー） |
| ドメイン | **ドメイン**（ウェブサイトの住所、example.com のようなもの） | **ドメイン**（サイトのURL） |
| SSL/HTTPS | **HTTPS**（通信を暗号化して安全にする仕組み） | **HTTPS**（暗号化通信） |
| キャッシュ | **キャッシュ**（一時的に覚えておく仕組み、2回目が速くなります） | **キャッシュ**（一時保存） |
| CDN | **CDN**（世界中にデータを置いて表示を速くする仕組み） | **CDN**（配信高速化） |

### 開発フロー用語

| 専門用語 | beginner | intermediate |
|----------|----------|--------------|
| レビュー | **レビュー**（他の人にコードを確認してもらうこと） | **レビュー**（コード確認） |
| CI/CD | **CI/CD**（変更するたびに自動でテストや公開をしてくれる仕組み） | **CI/CD**（自動テスト・デプロイ） |
| ステージング | **ステージング環境**（本番前のお試し環境） | **ステージング**（テスト環境） |
| 本番環境 | **本番環境**（実際にお客様が使う環境） | **本番**（実運用環境） |
| ロールバック | **ロールバック**（問題があったとき、前の状態に戻すこと） | **ロールバック**（巻き戻し） |

---

## レベル別の説明例

### 例1: エラーが発生した場合

**advanced:**
```
ビルドエラー: TypeScript の型エラーが src/utils.ts:42 で発生。
`string | undefined` を `string` に渡しています。
Optional chaining か型ガードで対応してください。
```

**intermediate:**
```
**ビルドエラー**（変換処理の失敗）が出ました。

src/utils.ts の 42行目で、値が空（**undefined**）になる可能性があるのに、
空ではない前提で使おうとしています。

**型ガード**（空かどうかのチェック）を追加すれば解決します。
```

**beginner:**
```
**ビルド**（コードを動かせる形に変換する処理）中に問題が見つかりました。

どういうこと？
→ ある場所で「値が入っていないかもしれない」のに、
  「必ず入っている」つもりで使おうとしていました。
  これを**型エラー**と呼びます（覚えておくと便利です）。

直すには？
→ 「もし値がなかったら〇〇する」という確認を追加します。

今、直しています。少々お待ちください。
```

### 例2: デプロイの確認

**advanced:**
```
本番環境へのデプロイを実行しますか？
Vercel に push 後、約2分で反映されます。
```

**intermediate:**
```
**デプロイ**（公開）を実行しますか？
**本番環境**（お客様が使うサイト）に反映されます。
```

**beginner:**
```
**デプロイ**（作ったものを公開して使えるようにすること）を実行しますか？

「はい」を選ぶと、**本番環境**（実際にお客様が使うサイト）が
新しくなります。

もし何か問題があったら、**ロールバック**（前の状態に戻すこと）が
できますのでご安心ください。
```

### 例3: Git の操作説明

**advanced:**
```
main ブランチに PR をマージします。
CI が通ったら自動デプロイされます。
```

**intermediate:**
```
**マージ**（統合）を実行します。
変更を**mainブランチ**（本流）に取り込みます。
**CI**（自動テスト）が通ったら、自動的に公開されます。
```

**beginner:**
```
**マージ**（今まで作業していた内容を本番に合流させること）を実行します。

流れ：
1. まず **CI**（自動でテストしてくれる仕組み）が動きます
2. テストに通ったら、自動で**デプロイ**（公開）されます
3. 何もしなくて大丈夫です。終わったらお知らせします

💡 「マージ」「CI」「デプロイ」は開発でよく使う言葉です。
   覚えておくと今後の会話がスムーズになりますよ。
```

---

## 文章スタイルの調整

### beginner

- 一文を短く（40文字以下を目安）
- 「〜とは？」の補足を入れる
- 箇条書きを多用
- 「です・ます」調
- 例え話や日常の言葉を使う
- 「大丈夫です」「心配ありません」などの安心感を与える表現

### intermediate

- 一文を適度に（60文字以下を目安）
- 専門用語は初出時に括弧で補足
- 「です・ます」調
- 必要に応じて技術的な詳細を追加

### advanced

- 簡潔に（冗長な説明は省略）
- 専門用語はそのまま使用
- 「だ・である」調も可
- 技術的な詳細を含める
- コマンドやコードを積極的に示す
